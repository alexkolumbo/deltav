"""Delta V — decentralized AI inference network.

A lightweight proof-of-stake chain, a vendor-agnostic compute layer
(NVIDIA / AMD today via llama.cpp, Groq and custom accelerators as
pluggable backends), and a smart router that matches HuggingFace models
to the VRAM actually available on the network's nodes.
"""

__version__ = "0.1.0"
